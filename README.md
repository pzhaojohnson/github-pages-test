# github-pages-test
Testing out GitHub pages

```typescript
type Spec = {
  sequence: string;
};

function RNAcanvas(spec: {}) {
  new RNAcanvasApp();

  [...spec.sequence].map(c => app.drawing.addBase(c));
}
```
